<%= title t('.title') %>

<%= content_for :top do %>
    <%= arrowleft_button_link_to "Volver a '#{project.title}'", project %>
<% end %>

<%= render project_proposals %>
<%= content_tag :p, t('.empty_project_proposals') if project_proposals.size == 0 %>