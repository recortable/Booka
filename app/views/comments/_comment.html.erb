<%= div_for comment do %>
    <div class="info">
      <%= avatar_img comment.user %>
      <%= comment.user.name %>,
      hace <%= time_ago_in_words comment.created_at %>
    </div>
    <%= comment.body %>
    <%= link_to t('.reply'), '#', :class => 'reply' %>
<% end %>